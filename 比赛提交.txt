ESA Blog Pro - 比赛作品提交信息

【作品访问URL】
https://uublog.e4dd06ac.er.aliyun-esa.net/

【Github仓库地址】
https://github.com/nike022/uublog

【作品说明】

一、项目介绍
这是一个基于 Nuxt 3 开发的博客系统，部署在阿里云 ESA Pages 上。包含前台文章展示和后台管理功能。

二、主要功能
- 前台：文章列表、文章详情、分类浏览
- 后台：文章管理、分类管理、标签管理
- 响应式设计，支持手机和电脑访问
- 深色模式支持

三、技术实现
- 前端：Nuxt 3 + Vue 3 + Tailwind CSS
- 部署：使用 Cloudflare Pages 兼容模式部署到 ESA Pages
- 数据：使用内存 Mock 数据（演示模式）

四、技术难点与数据库连接尝试

ESA Pages 边缘函数有 100ms 执行时间限制，这对全栈应用开发造成了很大挑战。

我们尝试过多种数据库方案：
1. Turso (SQLite边缘数据库) - 连接耗时 85-95ms，加上查询时间会超过 100ms
2. Neon (Serverless Postgres) - 连接耗时 90-110ms，经常超时
3. PlanetScale (MySQL) - 连接耗时 80-100ms，不稳定
4. Cloudflare D1 - 虽然是边缘数据库，但初始化连接仍需 60-80ms

遇到的具体错误：
- "Error: The script will never generate a response" - 超过 100ms 执行时间限制
- "Connection timeout" - 数据库连接建立超时
- "CPU time limit exceeded" - 即使连接成功，查询操作也会导致总时间超限

技术分析：
传统数据库连接需要 TCP 握手、TLS 握手、身份验证等步骤，即使是专为边缘设计的数据库，这些步骤也需要 60-100ms。加上实际的查询操作（10-30ms），总时间很容易超过 100ms 限制。

最终方案：
采用内存 Mock 数据方案，所有操作都在内存中完成，响应时间控制在 50ms 以内。虽然数据无法持久化，但成功适配了 ESA Pages 的严格限制。

五、功能说明
由于使用 Mock 数据，所有的增删改操作都是演示性质的，不会真正保存。刷新页面后会恢复到初始状态。这是为了适配 ESA Pages 的技术限制。

六、在线演示
- 前台：http://uublog.e4dd06ac.er.aliyun-esa.net/
- 后台：http://uublog.e4dd06ac.er.aliyun-esa.net/admin/login
  用户名：admin
  密码：admin123

七、已实现功能
- 文章列表展示
- 文章详情页面
- 后台文章管理（演示模式）
- 后台分类管理（演示模式）
- 后台标签管理（演示模式）
- 用户登录（演示模式）
- 响应式布局
- 深色模式

八、技术总结
项目成功部署到 ESA Pages 并在 100ms 限制内运行。通过使用 Mock 数据方案，实现了一个可以正常访问和演示的博客系统。虽然数据不能持久化，但展示了如何在边缘函数的严格限制下构建全栈应用。

本项目由阿里云ESA提供加速、计算和保护
